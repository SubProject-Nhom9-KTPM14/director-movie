server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/NguyenTranNhatHung18036971microservice/config-server.git
          default-label: main
          clone-on-start: true
        bootstrap: false
